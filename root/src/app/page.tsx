"use client";
// src/app/page.tsx
// Peak Tools Hub — Homepage
// Renders the full platform component (client-side interactive)
import PeakToolsHub from "@/components/PeakToolsHub";

export default function HomePage() {
  return <PeakToolsHub />;
}
